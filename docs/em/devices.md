# Device parameters

Generated by `python -m ic_opt.em.pcell.reference` from the config models of `builtin:clean_port` (geometry generation 7). Do not edit by hand.

Every length is in micrometres and every field name ends in `_um`; counts (`turns`, `secondary_turns`, `ring_count`) have no suffix. Metals are named as the process profile names its conductors (`"M6"`, or whatever a profile calls a metal, such as `"RDL"`); `"6"` stands for `"M6"`. The bottom metal, the profile's fixture conductor (`M1` unless `layer_catalog.ground_fixture_conductor` names another), is reserved for the ground fixture. Common to every family: `process_profile`, `port_order` (exactly the family's fixed port set), `ground_fixture` and `drc_check`.

## Ground fixture (`ground_fixture`)

| field | type | constraints | default |
| --- | --- | --- | --- |
| `inner_margin_um` | float | > 0 | required |
| `ring_width_um` | float | > 0 | required |
| `stub_width_um` | float or None | > 0 | none |
| `stub_length_um` | float | > 0 | required |
| `stub_chamfer_um` | float | >= 0 | required |
| `stub_width_by_port_um` | dict[str, float] or None |  | none |

## Patterned ground shield (`pgs`, ind_sym / xfm_bs / xfm_ms)

| field | type | constraints | default |
| --- | --- | --- | --- |
| `strip_width_um` | float | > 0 | required |
| `strip_spacing_um` | float | > 0 | required |
| `margin_um` | float | > 0 | required |

## `clean_port_ind_sym`

Ports: `P1, N1` (+ `CT` with `ct_metal`)

| field | type | constraints | default |
| --- | --- | --- | --- |
| `process_profile` | str |  | required |
| `port_order` | list[str] | min length 2 | required |
| `ground_fixture` | CleanPortGroundFixtureConfig |  | required |
| `drc_check` | bool |  | `True` |
| `outer_diameter_um` | float | > 0 | required |
| `width_um` | float | > 0 | required |
| `spacing_um` | float | > 0 | required |
| `opening_um` | float | > 0 | required |
| `lead_length_um` | float | > 0 | required |
| `turns` | int | >= 1 | required |
| `metal` | str | min length 1 | required |
| `port_spacing_um` | float or None | > 0, multiple of 0.01 | none |
| `pgs` | CleanPortPgsConfig or None |  | none |
| `straight_extension_um` | float | >= 0, multiple of 0.01 | `0` |
| `ct_metal` | str or None |  | none |

Retired names (refused with the replacement): `top_metal` → `metal`, `bottom_metal` (removed)

## `clean_port_xfm_bs`

Ports: `P1, N1, P2, N2` (+ `CTP` / `CTS` with `ct_primary_metal` / `ct_secondary_metal`)

| field | type | constraints | default |
| --- | --- | --- | --- |
| `process_profile` | str |  | required |
| `port_order` | list[str] |  | required |
| `ground_fixture` | CleanPortGroundFixtureConfig |  | required |
| `drc_check` | bool |  | `True` |
| `pgs` | CleanPortPgsConfig or None |  | none |
| `straight_extension_um` | float | >= 0, multiple of 0.01 | `0` |
| `primary_outer_diameter_um` | float | > 0 | required |
| `secondary_outer_diameter_um` | float | > 0 | required |
| `primary_width_um` | float | > 0 | required |
| `secondary_width_um` | float | > 0 | required |
| `primary_opening_um` | float | > 0 | required |
| `secondary_opening_um` | float | > 0 | required |
| `primary_lead_length_um` | float | > 0 | required |
| `secondary_lead_length_um` | float | > 0 | required |
| `center_spacing_um` | float | >= 0, multiple of 0.01 | required |
| `primary_metal` | str | min length 1 | required |
| `secondary_metal` | str | min length 1 | required |
| `ct_primary_metal` | str or None |  | none |
| `ct_secondary_metal` | str or None |  | none |
| `primary_port_spacing_um` | float or None | > 0, multiple of 0.01 | none |
| `secondary_port_spacing_um` | float or None | > 0, multiple of 0.01 | none |

## `clean_port_xfm_ms`

1:N stacked transformer. The primary is the single-turn winding (P1/N1, ``primary_width_um``); the secondary is the multi-turn winding (P2/N2, ``secondary_width_um``, ``secondary_turns`` >= 2).

Ports: `P1, N1` = the single-turn primary, `P2, N2` = the multi-turn secondary (+ `CTP` / `CTS`)

| field | type | constraints | default |
| --- | --- | --- | --- |
| `process_profile` | str |  | required |
| `port_order` | list[str] |  | required |
| `ground_fixture` | CleanPortGroundFixtureConfig |  | required |
| `drc_check` | bool |  | `True` |
| `pgs` | CleanPortPgsConfig or None |  | none |
| `straight_extension_um` | float | >= 0, multiple of 0.01 | `0` |
| `primary_outer_diameter_um` | float | > 0 | required |
| `secondary_outer_diameter_um` | float | > 0 | required |
| `primary_width_um` | float | > 0 | required |
| `secondary_width_um` | float | > 0 | required |
| `primary_opening_um` | float | > 0 | required |
| `secondary_opening_um` | float | > 0 | required |
| `primary_lead_length_um` | float | > 0 | required |
| `secondary_lead_length_um` | float | > 0 | required |
| `secondary_turns` | int | >= 2 | required |
| `secondary_spacing_um` | float | > 0 | required |
| `center_spacing_um` | float | >= 0, multiple of 0.01 | required |
| `primary_metal` | str | min length 1 | required |
| `secondary_metal` | str | min length 1 | required |
| `ct_primary_metal` | str or None |  | none |
| `ct_secondary_metal` | str or None |  | none |
| `primary_port_spacing_um` | float or None | > 0, multiple of 0.01 | none |
| `secondary_port_spacing_um` | float or None | > 0, multiple of 0.01 | none |

Retired names (refused with the replacement): `single_outer_diameter_um` → `primary_outer_diameter_um`, `multi_outer_diameter_um` → `secondary_outer_diameter_um`, `single_width_um` → `primary_width_um`, `multi_width_um` → `secondary_width_um`, `single_opening_um` → `primary_opening_um`, `multi_opening_um` → `secondary_opening_um`, `single_lead_length_um` → `primary_lead_length_um`, `multi_lead_length_um` → `secondary_lead_length_um`, `multi_turns` → `secondary_turns`, `multi_spacing_um` → `secondary_spacing_um`, `single_metal` → `primary_metal`, `multi_metal` → `secondary_metal`

## `clean_port_xfm_balun`

Ports: `P1, N1, P2, N2` (+ `CTP` / `CTS`)

| field | type | constraints | default |
| --- | --- | --- | --- |
| `process_profile` | str |  | required |
| `port_order` | list[str] |  | required |
| `ground_fixture` | CleanPortGroundFixtureConfig |  | required |
| `drc_check` | bool |  | `True` |
| `primary_outer_diameter_um` | float | > 0 | required |
| `secondary_outer_diameter_um` | float | > 0 | required |
| `primary_width_um` | float | > 0 | required |
| `secondary_width_um` | float | > 0 | required |
| `spacing_um` | float | > 0 | required |
| `primary_opening_um` | float | > 0 | required |
| `secondary_opening_um` | float | > 0 | required |
| `primary_lead_length_um` | float | > 0 | required |
| `secondary_lead_length_um` | float | > 0 | required |
| `primary_turns` | int | >= 1 | required |
| `secondary_turns` | int | >= 1 | required |
| `center_spacing_um` | float | >= 0, multiple of 0.01 | required |
| `metal` | str | min length 1 | required |
| `ct_primary_metal` | str or None |  | none |
| `ct_secondary_metal` | str or None |  | none |
| `primary_port_spacing_um` | float or None | > 0, multiple of 0.01 | none |

Retired names (refused with the replacement): `balun_metal` → `metal`

## `clean_port_xfm_tw`

Type 3 same-layer overlapping-inductor ("twisted") transformer: NR concentric octagon rings shared half-and-half by P (CCW) and S (P's x-mirror, CW), each NR/2 turns, connected across the NR-1 ring boundaries by explicit dive/same-layer legs. Both windings share ONE metal plane (``metal``) and ONE width (width_um) -- there is no separate primary / secondary width the way xfm_bs/xfm_ms/xfm_balun have, since P and S occupy the SAME rings and so share their width.

Ports: `P1, N1, P2, N2` (no taps)

| field | type | constraints | default |
| --- | --- | --- | --- |
| `process_profile` | str |  | required |
| `port_order` | list[str] |  | required |
| `ground_fixture` | CleanPortGroundFixtureConfig |  | required |
| `drc_check` | bool |  | `True` |
| `outer_diameter_um` | float | > 0 | required |
| `width_um` | float | > 0 | required |
| `spacing_um` | float | > 0 | required |
| `ring_count` | int |  | required |
| `port_gap_p_um` | float | > 0 | required |
| `port_gap_n_um` | float | > 0 | required |
| `lead_length_um` | float | > 0 | required |
| `metal` | str | min length 1 | required |

Retired names (refused with the replacement): `top_metal` → `metal`, `opening_p_um` → `port_gap_p_um`, `opening_n_um` → `port_gap_n_um`

## `clean_port_xfm_il`

Type 3 same-layer interleaved ("Rabjohn/Frlan") transformer: P and S alternate radial bands on ONE metal plane (``metal``), each turn's crossunder split across metal-1 (leg1) and metal-2 (leg2). Both windings share ONE width (width_um) and ONE spacing (spacing_um) -- unequal winding widths are not supported, the same rationale as xfm_tw's shared width_um; the CT tap leads also reuse that same shared W (the pcell's ``_il_ct_tap_exact`` draws every lead, tap included, at W).

Ports: `P1, N1, P2, N2` (+ `CTP` / `CTS`, upward or downward)

| field | type | constraints | default |
| --- | --- | --- | --- |
| `process_profile` | str |  | required |
| `port_order` | list[str] |  | required |
| `ground_fixture` | CleanPortGroundFixtureConfig |  | required |
| `drc_check` | bool |  | `True` |
| `outer_diameter_um` | float | > 0 | required |
| `width_um` | float | > 0 | required |
| `spacing_um` | float | > 0 | required |
| `turns` | int | >= 2 | required |
| `primary_opening_um` | float | > 0 | required |
| `secondary_opening_um` | float | > 0 | required |
| `primary_lead_length_um` | float | > 0 | required |
| `secondary_lead_length_um` | float | > 0 | required |
| `metal` | str | min length 1 | required |
| `ct_primary_metal` | str or None |  | none |
| `ct_secondary_metal` | str or None |  | none |

Retired names (refused with the replacement): `top_metal` → `metal`, `opening_p_um` → `primary_opening_um`, `opening_s_um` → `secondary_opening_um`, `lead_p_um` → `primary_lead_length_um`, `lead_s_um` → `secondary_lead_length_um`
